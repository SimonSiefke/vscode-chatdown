fileTypes: [chat]
name: Chatdown
patterns:
  - { include: "#frontMatter" }
repository:
  # the section where one defines the name of the bot and the name of the user(s)
  frontMatter:
    begin: \A # start of the file
    contentName: meta.embedded.block.frontmatter
    patterns:
      - { include: source.yaml }
    end: (^|\G)\s*$ # new line
scopeName: source.chatdown
uuid: 0A1D9874-B448-11D9-BD50-000D94B6E43C
